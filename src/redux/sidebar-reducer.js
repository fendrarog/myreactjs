let initialState = {
  friendsData: [
    { id: 1, name: "Iana" },
    { id: 2, name: "Pavlik" },
    { id: 3, name: "Duriman" },
  ],
};

const sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;
